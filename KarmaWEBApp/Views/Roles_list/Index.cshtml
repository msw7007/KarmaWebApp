@model IEnumerable<KarmaWEBApp.Models.roles_list>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Roles }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Roles }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Roles })
        </td>
    </tr>
}

</table>
